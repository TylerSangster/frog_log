<div>
  People were also interested in:

  <% resource.similar(3).each do |similar_resource| %>
    <div>
      <%= link_to similar_resource.name, similar_resource %>
    </div>
  <% end %>

</div>