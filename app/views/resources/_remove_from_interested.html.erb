<%= form_for(current_user.interests.find_by(resource_id: resource.id),
                                             html: { method: :delete },
                                             remote: true) do |f| %>
  <%= f.hidden_field :resource_id %>
  <%= f.submit "Unsave / Not Interested", class: "btn btn-danger btn-block" %>
<% end %>