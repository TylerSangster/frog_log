<%= form_for(current_user.interests.build(resource_id: resource.id), 
                                           remote: true) do |f| %>
  <%= f.hidden_field :resource_id %>
  <%= f.submit "Save / Interested", class: "btn btn-save btn-block" %>
<% end %>