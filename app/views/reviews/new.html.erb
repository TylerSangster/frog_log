<% provide(:title, 'Create a Review') %>

<%= form_for @review do |f| %>
	<%= render 'form', f: f %>
    <%= f.hidden_field :resource_id, :value => params[:review][:resource_id] %>
	
	<div>
		<%= f.submit "Create review" %>
	</div>
	
	<% end %>